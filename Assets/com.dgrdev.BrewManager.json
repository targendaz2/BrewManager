{
  "title": "Brew Manager Configuration (com.dgrdev.BrewManager)",
  "description": "Configure Homebrew via Brew Manager.",
  "__version": "1.0.0",
  "type": "object",
  "properties": {
    "HOMEBREW_ALLOWED_TAPS": {
      "title": "Allowed Taps",
      "description":
      "A list of taps. Homebrew will refuse to install a formula unless it and all of its dependencies are in an official tap or in a tap on this list.",
      "type": "array",
      "items": {
        "title": "Tap",
        "type": "string"
      }
    },
    "HOMEBREW_API_AUTO_UPDATE_SECS": {
      "title": "API Auto-update Seconds",
      "description":
      "Check Homebrew’s API for new formulae or cask data every $HOMEBREW_API_AUTO_UPDATE_SECS seconds. Alternatively, disable API auto-update checks entirely with $HOMEBREW_NO_AUTO_UPDATE.",
      "type": "number",
      "default": 450,
      "minimum": 0
    },
    "HOMEBREW_API_DOMAIN": {
      "title": "API Domain",
      "description":
      "Use this URL as the download mirror for Homebrew JSON API. If metadata files at that URL are temporarily unavailable, the default API domain will be used as a fallback mirror.",
      "type": "string",
      "default": "https://formulae.brew.sh/api"
    },
    "HOMEBREW_ARTIFACT_DOMAIN": {
      "title": "Artifact Domain",
      "description":
      "Prefix all download URLs, including those for bottles, with this value. For example, export `HOMEBREW_ARTIFACT_DOMAIN=http://localhost:8080` will cause a formula with the URL `https://example.com/foo.tar.gz` to instead download from `http://localhost:8080/https://example.com/foo.tar.gz`. Bottle URLs however, have their domain replaced with this prefix. This results in e.g. `https://ghcr.io/v2/homebrew/core/gettext/manifests/0.21` to instead be downloaded from `http://localhost:8080/v2/homebrew/core/gettext/manifests/0.21`",
      "type": "string"
    },
    "HOMEBREW_ARTIFACT_DOMAIN_NO_FALLBACK": {
      "title": "Artifact Domain No Fallback",
      "description":
      "When $HOMEBREW_ARTIFACT_DOMAIN and $HOMEBREW_ARTIFACT_DOMAIN_NO_FALLBACK are both set, if the request to $HOMEBREW_ARTIFACT_DOMAIN fails then Homebrew will error rather than trying any other/default URLs.",
      "type": "boolean"
    },
    "HOMEBREW_ASK": {
      "title": "Ask",
      "description":
      "If set, pass `--ask` to all formulae `brew install`, `brew upgrade` and `brew reinstall` commands.",
      "type": "boolean"
    },
    "HOMEBREW_AUTO_UPDATE_SECS": {
      "title": "Auto-update Seconds",
      "description":
      "Run `brew update` once every $HOMEBREW_AUTO_UPDATE_SECS seconds before some commands, e.g. `brew install`, `brew upgrade` or `brew tap`. Alternatively, disable auto-update entirely with $HOMEBREW_NO_AUTO_UPDATE.",
      "type": "number",
      "default": 86400,
      "minimum": 0
    },
    "HOMEBREW_BAT": {
      "title": "Bat",
      "description": "If set, use `bat` for the `brew cat` command.",
      "type": "boolean"
    },
    "HOMEBREW_BAT_CONFIG_PATH": {
      "title": "Bat Config Path",
      "description": "Use this as the bat configuration file.",
      "type": "string"
    },
    "HOMEBREW_BAT_THEME": {
      "title": "Bat Theme",
      "description": "Use this as the bat theme for syntax highlighting.",
      "type": "string"
    },
    "HOMEBREW_BOTTLE_DOMAIN": {
      "title": "Bottle Domain",
      "description":
      "Use this URL as the download mirror for bottles. If bottles at that URL are temporarily unavailable, the default bottle domain will be used as a fallback mirror. For example, `export HOMEBREW_BOTTLE_DOMAIN=http://localhost:8080` will cause all bottles to download from the prefix `http://localhost:8080/`. If bottles are not available at $HOMEBREW_BOTTLE_DOMAIN they will be downloaded from the default bottle domain.",
      "type": "string",
      "default": "https://ghcr.io/v2/homebrew/core"
    },
    "HOMEBREW_BREW_GIT_REMOTE": {
      "title": "Brew Git Remote",
      "description": "Use this URL as the Homebrew/brew `git(1)` remote.",
      "type": "string",
      "default": "https://github.com/Homebrew/brew"
    },
    "HOMEBREW_BREW_WRAPPER": {
      "title": "Brew Wrapper",
      "description": "If set, use wrapper to call `brew` rather than auto-detecting it.",
      "type": "string"
    },
    "HOMEBREW_BROWSER": {
      "title": "Browser",
      "description": "Use this as the browser when opening project homepages.",
      "type": "string"
    },
    "HOMEBREW_BUNDLE_USER_CACHE": {
      "title": "Bundle User Cache",
      "description": "If set, use this directory as the `bundle(1)` user cache.",
      "type": "string"
    },
    "HOMEBREW_CACHE": {
      "title": "Cache",
      "description": "Use this directory as the download cache.",
      "type": "string"
    },
    "HOMEBREW_CASK_OPTS": {
      "title": "Cask Options",
      "description":
      "Append these options to all cask commands. All `--*dir` options, `--language`, `--require-sha`, `--no-quarantine` and `--no-binaries` are supported. For example, you might add something like the following to your `~/.profile`, `~/.bash_profile`, or `~/.zshenv`:\n\n`export HOMEBREW_CASK_OPTS=\"--appdir=${HOME}/Applications --fontdir=/Library/Fonts\"`",
      "type": "string"
    },
    "HOMEBREW_CLEANUP_MAX_AGE_DAYS": {
      "title": "Cleanup Max Age Days",
      "description": "Cleanup all cached files older than this many days.",
      "type": "number",
      "default": 120,
      "minimum": 0
    },
    "HOMEBREW_CLEANUP_PERIODIC_FULL_DAYS": {
      "title": "Cleanup Periodic Full Days",
      "description":
      "If set, `brew install`, `brew upgrade` and `brew reinstall` will cleanup all formulae when this number of days has passed.",
      "type": "number",
      "default": 30,
      "minimum": 0
    },
    "HOMEBREW_COLOR": {
      "title": "Color",
      "description": "If set, force colour output on non-TTY outputs.",
      "type": "boolean"
    },
    "HOMEBREW_CORE_GIT_REMOTE": {
      "title": "Core Git Remote",
      "description": "Use this URL as the Homebrew/homebrew-core `git(1)` remote.",
      "type": "string",
      "default": "https://github.com/Homebrew/homebrew-core"
    },
    "HOMEBREW_CURLRC": {
      "title": "Curlrc",
      "description":
      "If set to an absolute path (i.e. beginning with `/`), pass it with `--config` when invoking `curl(1)`. If set but not a valid path, do not pass `--disable`, which disables the use of `.curlrc`.",
      "type": "string"
    },
    "HOMEBREW_CURL_RETRIES": {
      "title": "Curl Retries",
      "description": "Pass the given retry count to `--retry` when invoking `curl(1)`.",
      "type": "number",
      "default": 3,
      "minimum": 0
    },
    "HOMEBREW_CURL_VERBOSE": {
      "title": "Curl Verbose",
      "description": "If set, pass `--verbose` when invoking `curl(1)`.",
      "type": "boolean"
    },
    "HOMEBREW_DEBUG": {
      "title": "Debug",
      "description": "If set, always assume `--debug` when running commands.",
      "type": "boolean"
    },
    "HOMEBREW_DEVELOPER": {
      "title": "Developer",
      "description":
      "If set, tweak behaviour to be more relevant for Homebrew developers (active or budding) by e.g. turning warnings into errors.",
      "type": "boolean"
    },
    "HOMEBREW_DISABLE_DEBREW": {
      "title": "Disable Debrew",
      "description":
      "If set, the interactive formula debugger available via `--debug` will be disabled.",
      "type": "boolean"
    },
    "HOMEBREW_DISABLE_LOAD_FORMULA": {
      "title": "Disable Load Formula",
      "description":
      "If set, refuse to load formulae. This is useful when formulae are not trusted (such as in pull requests).",
      "type": "boolean"
    },
    "HOMEBREW_DISPLAY": {
      "title": "Display",
      "description":
      "Use this X11 display when opening a page in a browser, for example with `brew home`. Primarily useful on Linux.",
      "type": "string"
    },
    "HOMEBREW_DISPLAY_INSTALL_TIMES": {
      "title": "Display Install Times",
      "description": "If set, print install times for each formula at the end of the run.",
      "type": "boolean"
    },
    "HOMEBREW_DOCKER_REGISTRY_BASIC_AUTH_TOKEN": {
      "title": "Docker Registry Basic Auth Token",
      "description":
      "Use this base64 encoded username and password for authenticating with a Docker registry proxying GitHub Packages. If $HOMEBREW_DOCKER_REGISTRY_TOKEN is set, it will be used instead.",
      "type": "string"
    },
    "HOMEBREW_DOCKER_REGISTRY_TOKEN": {
      "title": "Docker Registry Token",
      "description":
      "Use this bearer token for authenticating with a Docker registry proxying GitHub Packages. Preferred over $HOMEBREW_DOCKER_REGISTRY_BASIC_AUTH_TOKEN.",
      "type": "string"
    },
    "HOMEBREW_DOWNLOAD_CONCURRENCY": {
      "title": "Download Concurrency",
      "description":
      "If set, Homebrew will download in parallel using this many concurrent connections. Setting to auto will use twice the number of available CPU cores (what our benchmarks showed to produce the best performance). If set to 1 (the default), Homebrew will download in serial.",
      "type": "number",
      "default": 1,
      "minimum": 1
    },
    "HOMEBREW_EDITOR": {
      "title": "Editor",
      "description":
      "Use this editor when editing a single formula, or several formulae in the same directory.\n\nNote: `brew edit` will open all of Homebrew as discontinuous files and directories. Visual Studio Code can handle this correctly in project mode, but many editors will do strange things in this case.",
      "type": "string"
    },
    "HOMEBREW_ENV_SYNC_STRICT": {
      "title": "Env Sync Strict",
      "description":
      "If set, `brew *env-sync` will only sync the exact installed versions of formulae.",
      "type": "boolean"
    },
    "HOMEBREW_EVAL_ALL": {
      "title": "Eval All",
      "description":
      "If set, brew commands evaluate all formulae and casks, executing their arbitrary code, by default without requiring `--eval-all`. Required to cache formula and cask descriptions.",
      "type": "boolean"
    },
    "HOMEBREW_FAIL_LOG_LINES": {
      "title": "Fail Log Lines",
      "description": "Output this many lines of output on formula `system` failures.",
      "type": "number",
      "default": 15,
      "minimum": 0
    },
    "HOMEBREW_FORBIDDEN_CASKS": {
      "title": "Forbidden Casks",
      "description":
      "A list of casks. Homebrew will refuse to install a cask if it or any of its dependencies is on this list.",
      "type": "array",
      "items": {
        "title": "Cask",
        "type": "string"
      }
    },
    "HOMEBREW_FORBIDDEN_FORMULAE": {
      "title": "Forbidden Formulae",
      "description":
      "A list of formulae. Homebrew will refuse to install a formula or cask if it or any of its dependencies is on this list.",
      "type": "array",
      "items": {
        "title": "Formula",
        "type": "string"
      }
    },
    "HOMEBREW_FORBIDDEN_LICENSES": {
      "title": "Forbidden Licenses",
      "description":
      "A list of SPDX license identifiers. Homebrew will refuse to install a formula if it or any of its dependencies has a license on this list.",
      "type": "array",
      "items": {
        "title": "License",
        "type": "string"
      }
    },
    "HOMEBREW_FORBIDDEN_OWNER": {
      "title": "Forbidden Owner",
      "description": "The person who has set any $HOMEBREW_FORBIDDEN_* variables.",
      "type": "string",
      "default": "you"
    },
    "HOMEBREW_FORBIDDEN_OWNER_CONTACT": {
      "title": "Forbidden Owner Contact",
      "description": "How to contact the $HOMEBREW_FORBIDDEN_OWNER, if set and necessary.",
      "type": "string"
    },
    "HOMEBREW_FORBIDDEN_TAPS": {
      "title": "Forbidden Taps",
      "description":
      "A list of taps. Homebrew will refuse to install a formula if it or any of its dependencies is in a tap on this list.",
      "type": "array",
      "items": {
        "title": "Tap",
        "type": "string"
      }
    },
    "HOMEBREW_FORBID_CASKS": {
      "title": "Forbid Casks",
      "description": "If set, Homebrew will refuse to install any casks.",
      "type": "boolean"
    },
    "HOMEBREW_FORBID_PACKAGES_FROM_PATHS": {
      "title": "Forbid Packages From Paths",
      "description":
      "If set, Homebrew will refuse to read formulae or casks provided from file paths, e.g. `brew install ./package.rb`.",
      "type": "boolean",
      "default": true
    },
    "HOMEBREW_FORCE_API_AUTO_UPDATE": {
      "title": "Force API Auto-update",
      "description":
      "If set, update the Homebrew API formula or cask data even if $HOMEBREW_NO_AUTO_UPDATE is set.",
      "type": "boolean"
    },
    "HOMEBREW_FORCE_BREWED_CA_CERTIFICATES": {
      "title": "Force Brewed CA Certificates",
      "description":
      "If set, always use a Homebrew-installed `ca-certificates` rather than the system version. Automatically set if the system version is too old.",
      "type": "boolean"
    },
    "HOMEBREW_FORCE_BREWED_CURL": {
      "title": "Force Brewed Curl",
      "description":
      "If set, always use a Homebrew-installed `curl(1)` rather than the system version. Automatically set if the system version of `curl` is too old.",
      "type": "boolean"
    },
    "HOMEBREW_FORCE_BREWED_GIT": {
      "title": "Force Brewed Git",
      "description":
      "If set, always use a Homebrew-installed `git(1)` rather than the system version. Automatically set if the system version of `git` is too old.",
      "type": "boolean"
    },
    "HOMEBREW_FORCE_BREW_WRAPPER": {
      "title": "Force Brew Wrapper",
      "description":
      "If set, require $HOMEBREW_BREW_WRAPPER to be set to the same value as $HOMEBREW_FORCE_BREW_WRAPPER for non-trivial brew commands.",
      "type": "string"
    },
    "HOMEBREW_FORCE_VENDOR_RUBY": {
      "title": "Force Vendor Ruby",
      "description":
      "If set, always use Homebrew’s vendored, relocatable Ruby version even if the system version of Ruby is new enough.",
      "type": "boolean"
    },
    "HOMEBREW_FORMULA_BUILD_NETWORK": {
      "title": "Formula Build Network",
      "description":
      "If set, controls network access to the sandbox for formulae builds. Overrides any controls set through DSL usage inside formulae. Must be `allow` or `deny`. If no value is set through this environment variable or DSL usage, the default behaviour is `allow`.",
      "type": "string",
      "enum": [
        "allow",
        "deny"
      ],
      "default": "allow",
      "options": {
        "enum_titles": [
          "Allow",
          "Deny"
        ]
      }
    },
    "HOMEBREW_FORMULA_POSTINSTALL_NETWORK": {
      "title": "Formula Postinstall Network",
      "description":
      "If set, controls network access to the sandbox for formulae postinstall. Overrides any controls set through DSL usage inside formulae. Must be `allow` or `deny`. If no value is set through this environment variable or DSL usage, the default behaviour is `allow`.",
      "type": "string",
      "enum": [
        "allow",
        "deny"
      ],
      "default": "allow",
      "options": {
        "enum_titles": [
          "Allow",
          "Deny"
        ]
      }
    },
    "HOMEBREW_FORMULA_TEST_NETWORK": {
      "title": "Formula Test Network",
      "description":
      "If set, controls network access to the sandbox for formulae test. Overrides any controls set through DSL usage inside formulae. Must be `allow` or `deny`. If no value is set through this environment variable or DSL usage, the default behaviour is `allow`.",

      "type": "string",
      "enum": [
        "allow",
        "deny"
      ],
      "default": "allow",
      "options": {
        "enum_titles": [
          "Allow",
          "Deny"
        ]
      }
    },
    "HOMEBREW_GITHUB_API_TOKEN": {
      "title": "GitHub API Token",
      "description":
      "Use this personal access token for the GitHub API, for features such as brew search. You can create one at https://github.com/settings/tokens. If set, GitHub will allow you a greater number of API requests.\n\nNote: Homebrew doesn’t require permissions for any of the scopes, but some developer commands may require additional permissions.",
      "type": "string",
      "links": [
        {
          "label": "More information",
          "url": "https://docs.github.com/en/rest/overview/rate-limits-for-the-rest-api"
        }
      ]
    },
    "HOMEBREW_GITHUB_PACKAGES_TOKEN": {
      "title": "GitHub Packages Token",
      "description":
      "Use this GitHub personal access token when accessing the GitHub Packages Registry (where bottles may be stored).",
      "type": "string"
    },
    "HOMEBREW_GITHUB_PACKAGES_USER": {
      "title": "GitHub Packages User",
      "description":
      "Use this username when accessing the GitHub Packages Registry (where bottles may be stored).",
      "type": "string"
    },
    "HOMEBREW_GIT_COMMITTER_EMAIL": {
      "title": "Git Committer Email",
      "description": "Set the Git committer email to this value.",
      "type": "string"
    },
    "HOMEBREW_GIT_COMMITTER_NAME": {
      "title": "Git Committer Name",
      "description": "Set the Git committer name to this value.",
      "type": "string"
    },
    "HOMEBREW_GIT_EMAIL": {
      "title": "Git Email",
      "description":
      "Set the Git author name and, if $HOMEBREW_GIT_COMMITTER_EMAIL is unset, committer email to this value.",
      "type": "string"
    },
    "HOMEBREW_GIT_NAME": {
      "title": "Git Name",
      "description":
      "Set the Git author name and, if $HOMEBREW_GIT_COMMITTER_NAME is unset, committer name to this value.",
      "type": "string"
    },
    "HOMEBREW_INSTALL_BADGE": {
      "title": "Install Badge",
      "description":
      "Print this text before the installation summary of each successful build.",
      "type": "string",
      "default": "🍺"
    },
    "HOMEBREW_LIVECHECK_AUTOBUMP": {
      "title": "Livecheck Autobump",
      "description":
      "If set, `brew livecheck` will include data for packages that are autobumped by BrewTestBot.",
      "type": "boolean"
    },
    "HOMEBREW_LIVECHECK_WATCHLIST": {
      "title": "Livecheck Watchlist",
      "description":
      "Consult this file for the list of formulae to check by default when no formula argument is passed to brew livecheck.",
      "type": "string"
    },
    "HOMEBREW_LOCK_CONTEXT": {
      "title": "Lock Context",
      "description":
      "If set, Homebrew will add this output as additional context for locking errors. This is useful when running `brew` in the background.",
      "type": "string"
    },
    "HOMEBREW_LOGS": {
      "title": "Logs",
      "description": "Use this directory to store log files.",
      "type": "string"
    },
    "HOMEBREW_MAKE_JOBS": {
      "title": "Make Jobs",
      "description":
      "Use this value as the number of parallel jobs to run when building with `make(1)`.",
      "type": "number",
      "minimum": 1
    },
    "HOMEBREW_NO_ANALYTICS": {
      "title": "No Analytics",
      "description": "If set, do not send analytics. Google Analytics were destroyed.",
      "type": "boolean",
      "links": [
        {
          "label": "More information",
          "url": "https://docs.brew.sh/Analytics"
        }
      ]
    },
    "HOMEBREW_NO_AUTOREMOVE": {
      "title": "No Autoremove",
      "description":
      "If set, calls to `brew cleanup` and `brew uninstall` will not automatically remove unused formula dependents.",
      "type": "boolean"
    },
    "HOMEBREW_NO_AUTO_UPDATE": {
      "title": "No Auto-update",
      "description":
      "If set, do not automatically update before running some commands, e.g. `brew install`, `brew upgrade` or `brew tap`. Preferably, run this less often by setting $HOMEBREW_AUTO_UPDATE_SECS to a value higher than the default. Note that setting this and e.g. tapping new taps may result in a broken configuration. Please ensure you always run `brew update` before reporting any issues.",
      "type": "boolean"
    },
    "HOMEBREW_NO_BOOTSNAP": {
      "title": "No Bootsnap",
      "description": "If set, do not use Bootsnap to speed up repeated `brew` calls.",
      "type": "boolean"
    },
    "HOMEBREW_NO_CLEANUP_FORMULAE": {
      "title": "No Cleanup Formulae",
      "description":
      "A list of formulae. Homebrew will refuse to clean up or autoremove a formula if it appears on this list.",
      "type": "array",
      "items": {
        "title": "Formula",
        "type": "string"
      }
    },
    "HOMEBREW_NO_COLOR": {
      "title": "No Color",
      "description": "If set, do not print text with colour added.",
      "type": "boolean"
    },
    "HOMEBREW_NO_EMOJI": {
      "title": "No Emoji",
      "description": "If set, do not print $HOMEBREW_INSTALL_BADGE on a successful build.",
      "type": "boolean"
    },
    "HOMEBREW_NO_ENV_HINTS": {
      "title": "No Env Hints",
      "description":
      "If set, do not print any hints about changing Homebrew’s behaviour with environment variables.",
      "type": "boolean"
    },
    "HOMEBREW_NO_FORCE_BREW_WRAPPER": {
      "title": "No Force Brew Wrapper",
      "description":
      "If set, disables $HOMEBREW_FORCE_BREW_WRAPPER behaviour, even if set.",
      "type": "boolean"
    },
    "HOMEBREW_NO_GITHUB_API": {
      "title": "No GitHub API",
      "description":
      "If set, do not use the GitHub API, e.g. for searches or fetching relevant issues after a failed install.",
      "type": "boolean"
    },
    "HOMEBREW_NO_INSECURE_REDIRECT": {
      "title": "No Insecure Redirect",
      "description":
      "If set, forbid redirects from secure HTTPS to insecure HTTP.\n\nNote: while ensuring your downloads are fully secure, this is likely to cause sources for certain formulae hosted by SourceForge, GNU or GNOME to fail to download.",
      "type": "boolean"
    },
    "HOMEBREW_NO_INSTALLED_DEPENDENTS_CHECK": {
      "title": "No Installed Dependents Check",
      "description":
      "If set, do not check for broken linkage of dependents or outdated dependents after installing, upgrading or reinstalling formulae. This will result in fewer dependents (and their dependencies) being upgraded or reinstalled but may result in more breakage from running `brew install _formula_` or `brew upgrade _formula_`.",
      "type": "boolean"
    },
    "HOMEBREW_NO_INSTALL_CLEANUP": {
      "title": "No Install Cleanup",
      "description":
      "If set, `brew install`, `brew upgrade` and `brew reinstall` will never automatically cleanup installed/upgraded/reinstalled formulae or all formulae every $HOMEBREW_CLEANUP_PERIODIC_FULL_DAYS days. Alternatively, $HOMEBREW_NO_CLEANUP_FORMULAE allows specifying specific formulae to not clean up.",
      "type": "boolean"
    },
    "HOMEBREW_NO_INSTALL_FROM_API": {
      "title": "No Install From API",
      "description":
      "If set, do not install formulae and casks in homebrew/core and homebrew/cask taps using Homebrew’s API and instead use (large, slow) local checkouts of these repositories.",
      "type": "boolean"
    },
    "HOMEBREW_NO_INSTALL_UPGRADE": {
      "title": "No Install Upgrade",
      "description":
      "If set, `brew install _formula_|_cask_` will not upgrade `_formula_|_cask_` if it is installed but outdated.",
      "type": "boolean"
    },
    "HOMEBREW_NO_UPDATE_REPORT_NEW": {
      "title": "No Update Report New",
      "description":
      "If set, `brew update` will not show the list of newly added formulae/casks.",
      "type": "boolean"
    },
    "HOMEBREW_NO_VERIFY_ATTESTATIONS": {
      "title": "No Verify Attestations",
      "description":
      "If set, Homebrew will not verify cryptographic attestations of build provenance for bottles from homebrew-core.",
      "type": "boolean"
    },
    "HOMEBREW_PIP_INDEX_URL": {
      "title": "Pip Index URL",
      "description":
      "If set, `brew install _formula_` will use this URL to download PyPI package resources.",
      "type": "string",
      "default": "https://pypi.org/simple"
    },
    "HOMEBREW_PRY": {
      "title": "Pry",
      "description": "If set, use Pry for the `brew irb` command.",
      "type": "boolean"
    },
    "HOMEBREW_SKIP_OR_LATER_BOTTLES": {
      "title": "Skip Or Later Bottles",
      "description":
      "If set along with $HOMEBREW_DEVELOPER, do not use bottles from older versions of macOS. This is useful in development on new macOS versions.",
      "type": "boolean"
    },
    "HOMEBREW_SORBET_RUNTIME": {
      "title": "Sorbet Runtime",
      "description":
      "If set, enable runtime typechecking using Sorbet. Set by default for $HOMEBREW_DEVELOPER or when running some developer commands.",
      "type": "boolean"
    },
    "HOMEBREW_SSH_CONFIG_PATH": {
      "title": "SSH Config Path",
      "description":
      "If set, Homebrew will use the given config file instead of `~/.ssh/config` when fetching Git repositories over SSH.",
      "type": "string"
    },
    "HOMEBREW_SUDO_THROUGH_SUDO_USER": {
      "title": "Sudo Through Sudo User",
      "description":
      "If set, Homebrew will use the $SUDO_USER environment variable to define the user to `sudo(8)` through when running `sudo(8)`.",
      "type": "boolean"
    },
    "HOMEBREW_SVN": {
      "title": "SVN",
      "description": "Use this as the `svn(1)` binary.",
      "type": "string"
    },
    "HOMEBREW_SYSTEM_ENV_TAKES_PRIORITY": {
      "title": "System Env Takes Priority",
      "description":
      "If set in Homebrew’s system-wide environment file (`/etc/homebrew/brew.env`), the system-wide environment file will be loaded last to override any prefix or user settings.",
      "type": "boolean"
    },
    "HOMEBREW_TEMP": {
      "title": "Temp",
      "description":
      "Use this path as the temporary directory for building packages. Changing this may be needed if your system temporary directory and Homebrew prefix are on different volumes, as macOS has trouble moving symlinks across volumes when the target does not yet exist. This issue typically occurs when using FileVault or custom SSD configurations.",
      "type": "string",
      "default": "/private/tmp"
    },
    "HOMEBREW_UPDATE_TO_TAG": {
      "title": "Update To Tag",
      "description":
      "If set, always use the latest stable tag (even if developer commands have been run).",
      "type": "boolean"
    },
    "HOMEBREW_UPGRADE_GREEDY": {
      "title": "Upgrade Greedy",
      "description": "If set, pass `--greedy` to all cask upgrade commands.",
      "type": "boolean"
    },
    "HOMEBREW_UPGRADE_GREEDY_CASKS": {
      "title": "Upgrade Greedy Casks",
      "description":
      "A list of casks. Homebrew will act as if --greedy was passed when upgrading any cask on this list.",
      "type": "array",
      "items": {
        "title": "Cask",
        "type": "string"
      }
    },
    "HOMEBREW_VERBOSE": {
      "title": "Verbose",
      "description": "If set, always assume `--verbose` when running commands.",
      "type": "boolean"
    },
    "HOMEBREW_VERBOSE_USING_DOTS": {
      "title": "Verbose Using Dots",
      "description":
      "If set, verbose output will print a `.` no more than once a minute. This can be useful to avoid long-running Homebrew commands being killed due to no output.",
      "type": "boolean"
    },
    "HOMEBREW_VERIFY_ATTESTATIONS": {
      "title": "Verify Attestations",
      "description":
      "If set, Homebrew will use the `gh` tool to verify cryptographic attestations of build provenance for bottles from homebrew-core.",
      "type": "boolean"
    },
    "SUDO_ASKPASS": {
      "title": "Sudo Askpass",
      "description": "If set, pass the `-A` option when calling `sudo(8)`.",
      "type": "boolean"
    },
    "all_proxy": {
      "title": "All Proxy",
      "description":
      "Use this SOCKS5 proxy for `curl(1)`, `git(1)` and `svn(1)` when downloading through Homebrew.",
      "type": "string"
    },
    "ftp_proxy": {
      "title": "FTP Proxy",
      "description":
      "Use this FTP proxy for `curl(1)`, `git(1)` and `svn(1)` when downloading through Homebrew.",
      "type": "string"
    },
    "http_proxy": {
      "title": "HTTP Proxy",
      "description":
      "Use this HTTP proxy for `curl(1)`, `git(1)` and `svn(1)` when downloading through Homebrew.",
      "type": "string"
    },
    "https_proxy": {
      "title": "HTTPS Proxy",
      "description":
      "Use this HTTPS proxy for `curl(1)`, `git(1)` and `svn(1)` when downloading through Homebrew.",
      "type": "string"
    },
    "no_proxy": {
      "title": "No Proxy",
      "description":
      "A list of hostnames and domain names excluded from proxying by `curl(1)`, `git(1)` and `svn(1)` when downloading through Homebrew.",
      "type": "array",
      "items": {
        "title": "Hostname/Domain",
        "type": "string"
      }
    }
  }
}
